{
  "name": "tekstura-stairs-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static Tekstura stairs website QA helpers.",
  "scripts": {
    "check": "npm run check:js",
    "check:js": "node --check stair-configurator.js && node --check stair-geometry-engine.js && node --check stair-materials.js && node --check stair-formulas.js && node --check api/notify-lead.js && node --check calculator-critical-fixes.js"
  }
}
